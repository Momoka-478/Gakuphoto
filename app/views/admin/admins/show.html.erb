<div>
  <h3><%= @admin.name %></h3>
  <% if @admin == current_admin.id %>
    <p><%= link_to "プロフィール編集", edit_admin_path(@admin) %></p>
  <% end %>
</div>

<% @posts.each do |post| %>
<div>
  <%= link_to post_path(post.id) do %>
    <%= image_tag post.get_image %>
  <% end %>
  <p>タイトル：<%= post.title %></p>
  <p>一言：<%= post.body %></p>
  <p>投稿者：<%= post.admin.name %></p>
</div>
<% end %>